# Getting User Input

# we use built in function called input
# once the user enters the input and press enter it gets stored in
# the variables


name = input("Enter Your Name: ")
print(f"Welcome, {name}")

# input is always a string